@extends('layouts.app')
@section('title','Logbook Bimbingan')
@section('content')
<div class="page-head"><div><h1>Logbook Bimbingan</h1><div class="muted">Catat progres bimbingan dan tautan revisi.</div></div></div><div class="grid two-col"><section class="card"><h2 style="margin-top:0">Tambah Logbook</h2><form method="POST" action="{{ route('logbook.store') }}" class="form-grid">@csrf<div class="field"><label>Tanggal</label><input class="input" type="date" name="tanggal" value="{{ old('tanggal',date('Y-m-d')) }}" required></div><div class="field"><label>Link Revisi / Google Drive</label><input class="input" type="url" name="file_url" value="{{ old('file_url') }}" placeholder="https://drive.google.com/..."></div><div class="field full"><label>Uraian Bimbingan</label><textarea class="textarea" name="uraian" required>{{ old('uraian') }}</textarea></div><div class="field full"><button class="btn btn-primary" type="submit">Simpan Logbook</button></div></form></section><aside class="card"><h2 style="margin-top:0">Riwayat</h2><div class="list">@forelse($rows as $row)<div class="list-item"><strong>{{ $row['tanggal'] }}</strong><p>{{ $row['uraian'] }}</p><span class="badge {{ ($row['status_approval']??'')==='Disetujui'?'badge-success':'badge-warning' }}">{{ $row['status_approval']??'Menunggu' }}</span>@if(!empty($row['catatan_dosen']))<p class="muted"><strong>Catatan dosen:</strong> {{ $row['catatan_dosen'] }}</p>@endif</div>@empty<div class="muted">Belum ada logbook.</div>@endforelse</div></aside></div>
@endsection